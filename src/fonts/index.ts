import { inter, injectInterFont } from './inter';
import { calsans, injectCalSansFont } from './calsans';

export { inter, injectInterFont, calsans, injectCalSansFont };

// // Eliminamos Segoe UI por ahora
// export const segoe = Segoe_UI({
//   subsets: ['latin'],
//   weight: ['400', '600', '700'], // Ajusta los pesos según necesites
//   variable: '--font-segoe', // Variable CSS para texto general
// }); 